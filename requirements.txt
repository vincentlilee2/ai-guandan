fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-dotenv>=1.0.0
openai>=1.10.0
httpx>=0.26.0
pydantic>=2.5.3
# 可选：GAME_STORE_BACKEND=redis 持久化时需要（对局状态跨重启恢复）
redis>=4.6.0
# 注：pymysql 已移至 requirements-mysql.txt（独立版默认不连 MySQL，按需安装 extras）
